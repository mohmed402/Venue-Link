import React from "react";
import "../styles/Loader.css"; // Import CSS Module

const Loader = () => {
  return (
    <div className="loaderWrapper">
      <div className="loader" />
    </div>
  );
};

export default Loader;
